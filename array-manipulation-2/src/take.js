/* exported take */
// define function named take that takes two arguments (array) and (count)
// return array with slice method, start the slice at 0 and end at count
// returns array with elements from 0 index to count index
function take(array, count) {
  return array.slice(0, count);
}
