/* exported setValue */
function setValue(object, key, value) {
  object[key] = value;
}

// define new function named setValue with three parameters(object,key,value)
// set up function block to have each parameter go to correct spot
// function will not have a return
// function allows user to input new values inside object with new key name
