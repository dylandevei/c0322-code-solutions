/* exported getValue */
function getValue(object, key) {
  var value = object[key];
  return value;
}

// define function named getValue with two parameters, object and name
// declare new variable (value) to equal the object at key index
// key will return the value of inside object
// return value variable
