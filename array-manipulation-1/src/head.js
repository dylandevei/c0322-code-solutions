/* exported head */

// define function with one parameter (array)
// declare firstIndex variable and index array at 0 , array[0]
// return head variable of array[0]

function head(array) {
  var firstIndex = array[0];
  return firstIndex;
}
