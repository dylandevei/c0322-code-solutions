/* exported last */

// define function named last with single parameter(array)
// declare lastIndex variable to equal array[array.length-1]
// return lastIndex variable to find last index value of the array

function last(array) {
  var lastIndex = array[array.length - 1];
  return lastIndex;
}
