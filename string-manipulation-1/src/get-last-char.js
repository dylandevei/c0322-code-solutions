/* exported getLastChar */
function getLastChar(string) {
  var lastCharacter = string[[string.length - 1]];
  return lastCharacter;
}

// define function to return last character of string
// achieve this by indexing string at string.length-1
// if string is cat, should output 't'
// create variable that equals last letter of string
//  return variable with result of string at length-1 index
