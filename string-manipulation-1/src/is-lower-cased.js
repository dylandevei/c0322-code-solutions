/* exported isLowerCased */
function isLowerCased(word) {
  if (word.toLowerCase() === word) {
    return true;
  } else {
    return false;
  }
}
// define function that checks if word is all lowercase
// if word all lowercase equals value of word return true
// if word does not equal all uppercase value return false
