/* exported getFirstChar */
function getFirstChar(string) {
  var firstCharacter = string[0];
  return firstCharacter;
}

// define function to return first character [0] of string
// if string is cat, should output 'c'
// create variable that equals first letter of string
//  return variable with result of string at 0 index
